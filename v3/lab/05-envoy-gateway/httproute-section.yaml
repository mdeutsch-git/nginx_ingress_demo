# Patch to add /echo route to the EG HTTPRoute
# The full HTTPRoute is in envoy-gateway.yaml — this is for reference
# Replace the rules section in envoy-gateway.yaml HTTPRoute with:
#
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /echo
#       backendRefs:
#         - name: echoserver
#           port: 80
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: httpbin
#           port: 80
